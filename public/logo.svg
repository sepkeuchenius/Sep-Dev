<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Outer circle (larger) -->
  <circle cx="50" cy="50" r="45" fill="#C5D89D"/>
  <!-- Inner circle (smaller) -->
  <circle cx="50" cy="50" r="25" fill="#9CAB84"/>
</svg>

